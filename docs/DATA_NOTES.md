# DATA NOTES — 데이터 품질 기록

> 후처리/학습에서 **반드시 참고**할 데이터 신뢰성 이슈를 날짜·범위별로 기록.
> 오염/누락/노선개편 등 "이 데이터는 이래서 믿으면 안 된다"를 남긴다.

## 2026-05-28 — 운영 메모

### 15:13~15:19 ITS 6분 outage (`.74` IP 휘발)
- 인터넷 일시단절 복구 시 비영속 보조 IP `210.117.134.74` 가 `enp6s0` 에서 소멸 → 수집기가 사라진 IP 에 바인딩 시도 → ITS(버스+교통) 전량 실패 (`cannot assign requested address`).
- 영향: bus 6분, traffic 6분(파일 237B 실패마커 5개) 결측. weather/incident 무영향(다른 출구).
- 복구: `.74` 재부착 → ITS 200 확인 → 수집기 재시작. trip 재구성 입장에선 해당 6분 ord 전이 결측으로 흡수(보간 안 함) → y라벨 오염 0, 일부 segment 만 비움.
- ⚠️ **재발 위험**: 인터넷 단절·리부팅마다 동일 사고. 자동복구 옵션(systemd ExecStartPre 로 IP 재부착) 미적용 — [STATUS](STATUS.md) 막힌 것/임시조치 참조.

### 17:37 시스템 타임존 UTC → Asia/Seoul 전환
- 발견: `logs/*.log` 일자 회전이 **KST 09:00** 에 일어남 (예: `bus.log.20260527` 가 5/28 08:59 까지 추가됨). 원인 = 시스템 TZ=`Etc/UTC` + `TimedRotatingFileHandler(when="midnight")` 의 기본 동작이 *시스템 localtime* 자정 회전 → UTC 자정 = KST 09:00.
- 영향 범위: **로그 파일 회전 시점뿐.** 데이터(ts·hour_key·date_key·trip service_date) 는 전부 `src/common/clock.py` 의 명시적 KST 처리라 무영향.
- 조치: ① `sudo timedatectl set-timezone Asia/Seoul` ② `src/common/log.py` 에 `_KSTMidnightRotatingFileHandler` subclass 추가(시스템 TZ 무관하게 KST 자정 회전·KST 일자 suffix 강제 — 향후 TZ 가 어떤 이유로 다시 UTC 가 돼도 로그는 KST 기준 유지) ③ `blog-collector` 재시작. 다음 회전은 2026-05-29 00:00 KST 예정.
- 부수효과: 시스템 전역 TZ 변경 — 같은 서버의 cron·systemd timer·다른 사용자 로그 회전도 KST 기준이 됨. (서버가 한국 소재이므로 의도된 정합)
- 기존 UTC-자정 회전 백업 파일 재분할: 라인별 KST timestamp 파싱 → `logs/*.log.YYYYMMDD` 를 KST 일자로 재그루핑(원본은 `*.bak_utc` 로 보존). 활성 파일 앞에 KST 5/28 09:00 이전 분량 prepend 해서 활성 = KST 5/28 전체 분량 → 다음 회전 시 덮어쓰기 충돌 없이 `*.log.20260528` 로 정확히 회전. (collector ~10초 stop, polling 1 cycle 손실 — 데이터 영향 미미)

### 04:38~05:03 첫차 전 차고지 telemetry leak
- 새벽 시간(00~04:37 idle=892) 후 04:38부터 25분간 차량 2대 telemetry 일시 등장 → 05:04~05:40 다시 idle → 05:41 본격 첫차 ramp-up.
  - plate **1579** (제일여객, 305001660 노선): ord=2 stuck 23분, GPS 289m 범위, 저속 10~34km/h → 차고지 출고·warm-up 회전
  - plate **1719** (호남고속, 305001763 노선): ord=1 stuck 2분, GPS 32m, 시동만 잠깐
- trip 재구성: ord 전이 0 → segment 0 → drop (정상, 5/27 right-censoring 3노선과 동일 메커니즘).
- **운영 패턴**: 출고대기 차량의 telemetry 가 본격 운행 전 잠깐 새는 정상 현상. 학습 무영향 (drop).

## 2026-05-27 — 첫 평일 풀데이터(00~24시) trip 재구성 점검 (재집계 완료)
> 18:14~18:18 5분 ITS outage(.74 IP 소실) 외 전 시간대 수집. 5/28 시점 재집계.

### 수집→학습 funnel
| 단계 | 개수 | 비고 |
|---|---|---|
| RAW 차량관측 | 1,719,458 | poll 요청 1,794,720, 446노선 × 24h |
| 시간표 슬롯(예정 발차) | 4,466 | 5/27 평일 |
| 재구성 trip | 4,290 | 96% of 슬롯 |
| **1차 학습용 trip** | **4,063** | **91% of 슬롯** (on_schedule=True ∩ segments≥1) |
| **학습용 segment(y라벨)** | **184,595** | ord 고신뢰 98.4% + gps복원 1.6% |
| 추가 손실 (복원불가) | 2,047 segment | 전체의 1.07% |

trip 4290 분기: on_schedule **True 94.7%(4063)**, False(off-sched) 1.6%(70), None(mid_entry, 발차검출 X) 3.7%(157). 종점도달 trip 3834 (**89.4%**, 17h시점 83%↑). 발차매칭 median 24s, ≤60s 86%, ≤180s 97%.

### 미매칭 슬롯 403개(9%) 분해 — 절대수 기준
- **정상사유 88개(2.0%)**: 미운행 노선 슬롯 79 + 우측절단/off-route 노선 슬롯 9
- **검토필요 315개(7.0%)**: trip 만든 노선에서 빠진 슬롯 (= 진짜 검출누락 or 결행)
  - 시간대 균등 분포(16~34/시간), 출퇴근 피크 차이 없음 → 시각 의존성 X
  - 186/446 노선이 누락 보유 → 흩어져 있음
  - 다중일 비교 누적되면 결행(노선·시각 특정) vs 검출누락(임계값 R/K 문제) 구분 가능

### trip 0 노선 36개 분해
- **raw obs 0 (미운행) 33노선**, 시간표 검증:
  - **평일슬롯 0 (시간표상 미운행 등록) 2개**: 305001721·747 (76번 양방향), 토/공휴일 슬롯도 0 → 사실상 미운영 등록만
  - **평일슬롯 있는데 obs 0 31개**:
    - 이벤트/한정 (시간표에 명시): 8490번(만경강 벚꽃길, 4.3~4.5 한정), 1994번(전북현대 홈경기 한정) — 5/27 운영 X (정상)
    - 외곽·저빈도 (평일슬롯 1~10개 수준): 21·26·70·82-1·84·85번 → 시간표엔 있어도 실제 결행 가능 (다중일로 패턴 확인 필요)
- **관측있음 3노선 (우측절단 잔존)**: 305001720(plate 1211, 17:39 ord=1 stuck 21폴), 305001724(plate 1203, 13:53 ord=1 6폴), 305001746(plate 1203, 12:21 ord=2 1폴). 풀데에서도 ord 전이 0 → segment 0 → 드롭. **차고지 출고대기 telemetry leak** (5/28 새벽 plate 1579·1719와 동일 패턴).

### off-schedule 발차 70개(1.7%) — 학습 정책: **1차 drop**
- 가장 가까운 미매칭 슬롯까지 거리: median **31분**, p90 **56분**, max 306분
- ≤10분 4%, ≤20분 19% → MATCH_GATE(600s) 늘려도 못 잡힘 = 게이트 튜닝 무의미
- 즉 **시간표 외 비정규 운행** (임시운행/회송/결행 메꿈). 손님 승하차 불명 = 정류장 정차시간이 정규와 다를 수 있음 → 학습 노이즈
- **1차 모델은 시간표 슬롯 기반 사전추론**이므로 슬롯 매핑 없는 trip 은 X feature 부족 → drop. **2차 모델(실시간 Transformer)** 은 별개 판단.

### plate 1203 — "누수" 단정 보류, "(plate, company) 키 충돌" 가능성 양립
- 5/27 plate 1203 폴 60개 전부 회사=시민여객 (3노선 분포: 305001681 53폴, 305001724 6폴, 305001746 1폴)
- **두 시나리오 데이터로 구분 불가**:
  1. 시민여객에 4자리 1203 으로 끝나는 버스가 두 대 (예: 31바1203, 31사1203) → (plate-4자리, company) 키도 같음 = 학습 ID 충돌
  2. 한 대인데 API 가 다른 stdid 응답에 1203 폴을 끼워보냄 (이전 305001677 ord45 사례와 동일)
- 결정적 증거 1건: 305001681 ord=25,26 (terminus 21 초과 = 그 노선 위 아님) → 어느 시나리오든 **하드드롭(ord > terminus) 작동해 학습 무영향**
- 정확한 식별이 필요해지면 PLATE_NO 4자리 외 추가 식별자(한글 prefix '바/사' 등) 필요. raw 응답에 4자리만 와서 현재 한계.

### ord얼음 GPS 복원 — trip 레벨 손상도
| | 개수 | 비율 |
|---|---|---|
| 깨끗 (얼음 0) | 3,052 | 71.1% |
| 얼음 있었으나 GPS로 전부 살림 | 577 | 13.4% |
| 복원불가 보유 | **661** | **15.4%** |
| └ ≥5개 못살림 (꽤 손상) | 127 | 3.0% |
| └ ≥10개 못살림 (심각) | 75 | 1.7% |

복원불가 661 trip 도 trip 자체는 살아있음(일부 segment 만 비어있음). 잃은 segment 총 **2,047 / 전 segments 191,616 = 1.07%** 학습 신호 손실. trip당 얼음 median 2, p90 7, max 48.

### 결론
- **5/27 풀데 깨끗**. 시간표 슬롯 91%(4,063)가 1차 학습 trip 으로, 18.5만 segment 가 y라벨로 살아남음. 신호 손실 trip 9% + segment 1% 수준.
- **남은 검토거리 4개**: (a) 미매칭 검출누락 315 정밀분해(다중일), (b) trip0 31노선의 결행 vs 의도된 미운행 구분(다중일), (c) plate 4자리 충돌 식별 한계, (d) ord얼음 ≥10 trip 75개의 노선/회사 편향.

## 2026-05-26 — 사용 금지 (오염 가능)
- 이 날 BLog 수집기 셋업·스모크 테스트 중, **같은 머신/공인 IP에서 수집기 3개가 동시 가동**
  (yubin 132@5s, gaeun 446@5s, BLog 테스트 446@5s)되어 ITS 서버 throttle 발생.
- 결과: timeout / SERVER_DISCONNECTED / 연결오류 다발 → **버스 위치 데이터에 다량의 결측·지연**.
- 또한 정적 데이터 정비 이전 시간대라 수집 의미도 적음.
- **결론: 2026-05-26 raw(bus/traffic 등)는 학습/후처리에 사용하지 않는다.**
- 같은 날 yubin/gaeun 기존 수집기를 중단하고 BLog 수집기 단독 체제로 전환.

## 2026-05-26 오후 — ITS IP 차단 (수집 일시 중단)
- 오늘 셋업/부하테스트 누적(수집기 3개 동시 + conc 100/200 버스트 테스트 다수)으로 **ITS 서버(its.jeonju.go.kr, 115.92.162.200)가 우리 공인 IP 를 차단**. 증상: TCP connect timeout(SYN 드롭), DNS·KMA(apis.data.go.kr)는 정상.
- 차단 식히려 수집기(systemd) stop. 재가동은 ITS 443 연결 회복 확인 후.
- **교훈**: 부하 실험은 짧고 신중히. 단독 운영 시에도 446@5s=89req/s 는 yubin 의 검증된 26/s(132@5s) 대비 3.4배 → 지속 운영 시 rate-cap 위험. 재가동 시 rate 정책(페이싱 유지/시간표필터/interval↑) 재검토 필요.

## 날씨 수집 상태 (2026-05-26 기준)
- 실황(getUltraSrtNcst)·초단기예보(getUltraSrtFcst)·단기예보(getVilageFcst): **정상**, 43격자 거의 전부 ok.
- 중기예보 longForecast(getMidLandFcst/getMidTa, MidFcstInfoService): **HTTP_403**.
  - 원인: 현재 KMA 키가 "중기예보 조회서비스" API 에 **미구독**. (data.go.kr 는 API별 활용신청이 따로)
  - 영향: 중기(3~10일)는 버스 ETA 에 거의 무관 → 비차단. 필요 시 data.go.kr 에서 해당 서비스 활용신청하면 코드 수정 없이 자동 수집됨.

## reference stops — ord 공간 & 종점 ord (2026-05-26 trip v1.1 검증)
- **stops 레코드엔 ord 가 둘**: `ROUTE_ORD`(노드기반, 비정류장 노드 포함 → 결번 존재, 446중 76노선) vs `STOP_ORD`(정류장 연속 1..N, 전 446노선 결번 0 = len).
- **버스 API `LATEST_STOP_ORD` = STOP_ORD 공간**(검증: 305200113 ROUTE_ORD 결번 32·34·36·39 가 버스 관측값에 그대로 나타나고 버스값이 연속 1..N → STOP_ORD 와 일치). 따라서 **종점 ord = max(STOP_ORD)**. ROUTE_ORD 를 쓰면 종점 과대추정 + 버스 ord 와 어긋남(초기 v1.1 ② 오류였음, 수정함).
  - 검증: 305200112 종점 STOP_ORD 43 = "우석대종점" = timetable `BRT_ENAME` 일치(reference 신뢰 가능, 명칭은 맞고 ord 만 ROUTE_ORD 48 로 잘못 봤던 것).
  - 5/26 저녁 부분데이터: reference 종점 vs 실측 최대 end_ord median gap=1(ord 의미상 종점 도착 직전 N-1 에서 소멸 → 정상), gap≥5 노선 25개(=저녁창에 종점근접 trip 미포착, 전일 데이터로 해소 예상).
- ✅ **305001677 (83번, 종점 33) ord45 정체 규명·해결**: plate 1203 이 동시각에 305001676·305001524·305001677 세 노선에 서로 다른 좌표로 등장(아래 PLATE_NO 비유일 참조) = **API 누수/번호중복**. ord 34~45 는 reference 종점(33) 초과 불가능값 → **load 단계 하드드롭**(거리무관 안전, 전노선 6관측만 해당). 잔여 off-route 는 `off_route_obs` flag.

## PLATE_NO 비유일 + off-route 관측 (2026-05-26 raw 전체 점검)
- **PLATE_NO = 4자리 숫자만**(한글/지역 prefix 없음, 전수 확인). "31바1206"·"31사1206" 둘 다 "1206" → **전역 unique 아님**. 같은 poll 내 동일 plate 중복은 0(노선내 매칭 안전), 동시각 복수노선 등장 12 plate 는 전부 노선間(번호 같은 다른 버스 = 정상, 노선별 재구성이라 무해).
- **복수노선 plate 320개 중 308 은 순차(시종점서 노선 바꿔 운행=정상)**, 12 만 동시겹침. plate 단독 신뢰 금지 → 지오메트리 교차검증 병행.
- **off-route 관측**: 운행종료 후 차고지 회송/주박 시 ord 가 종점값에 얼어붙은 채 GPS 가 정류장서 멀어짐(최대 4.8km). 노선 최대 인접정류장 간격(전노선 최대 6990m)을 넘으면 off-route 로 `off_route_obs` 카운트(노선적응형 — 벽지노선 정상 mid-segment 과삭제 회피, **flag 만**). 5/26 4.7% trip 해당.

## LATEST_STOP_ORD telemetry 불량 — 얼었다 튐 (2026-05-26 검증)
- 일부 차량(특히 장거리·시외성 노선)에서 **`LATEST_STOP_ORD` 가 한 값에 수분간 얼어붙었다가 여러 정류장을 건너뛰어 점프**한다. GPS 좌표로 검증 시 버스는 그 사이 정류장들을 **물리적으로 정상속도(~4분)로 통과**했음 → "빠른 통과"도 "수집공백"도 아닌 **원천 telemetry 갱신주기가 폴링(10초)보다 느린 차량**의 문제(GPS 위치도 30~50초 얼었다 600~1000m 점프).
  - 실례: 305001224 plate 1352, ord 3 에 4분(20:48~20:52) 고정 — 그동안 GPS 는 슬치마을·산정마을·원용암·춘향로용암(STOP_ORD 4~7) 좌표를 통과 — 이후 ord 8 로 점프.
- **분포**: trip 1169 중 943(81%)은 ord 점프 없음(증가폭 1). 226(19%)이 점프≥2 보유(대부분 2~3, 큰 점프 10+는 7 trip).
- **현재 처리 = 안전(무손실 아닌 무오염)**: `extract_segments` 는 `n+1` 관측 시에만 구간 생성 → 점프 구간(3→8)에 **가짜 긴 구간을 만들지 않고 비운다**. y라벨 오염 0, 단 해당 구간 결측(전체 구간의 3.4%). stops 도 비연속(…1,2,3,8,14…)로 정직 기록.
- 작년 BIS_APP 은 이 문제를 **보간**(ORD 2 등 보간 + ORD1 시간표 하드고정)으로 메웠으나 → *실제 출발/구간 오염*. 이번엔 보간 안 함(설계 §4.5). 향후 옵션: GPS↔정류장 좌표 근접매칭으로 결측 구간 복원(v1.2/v2 검토).

## ord 공간 정정 메모 (2026-05-26)
- 종점/구간 ord 는 **STOP_ORD** 사용(작년 BIS_APP 도 동일 — `mapLastStop.py`·`trackSingleBus.py`·`generateFirstETA.py` 전부 `max(STOP_ORD)`). `ROUTE_ORD` 는 노드기반 결번필드로 **작년 코드에서도 미사용**, 정체불명. (v1.1 ② 초기 구현이 ROUTE_ORD 로 잘못 갔다가 정정)

## 정적 데이터 기준
- stdid 총 **446개** (2026-05-26 API 기준). 작년(2025) 451 에서 노선 개편으로 -5.
- 정적 원본은 `data/reference/source/` (fetch 시각은 각 파일 `fetched_at`).
