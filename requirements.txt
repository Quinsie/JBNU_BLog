# ── 수집기 (Phase 1) ─────────────────────────────────
aiohttp>=3.9
requests>=2.31
python-dotenv>=1.0
APScheduler>=3.10
tzdata>=2024.1

# ── 후처리 (Phase 3) ─────────────────────────────────
holidays>=0.40   # 한국 공휴일(음력 명절·대체공휴일) → trip daytype 분류

# ── serve API (Phase 7) ──────────────────────────────
fastapi>=0.110            # 앱-facing API. 코드가 곧 OpenAPI/Swagger(/docs)
uvicorn[standard]>=0.29   # ASGI 서버
pydantic>=2.6             # 요청/응답 스키마

# ── ML (Phase 4~) : 필요 시점에 활성화 ──────────────
# pandas>=2.2
# pyarrow>=16
# numpy>=1.26
# scikit-learn>=1.4
# lightgbm>=4.3
# torch>=2.2
